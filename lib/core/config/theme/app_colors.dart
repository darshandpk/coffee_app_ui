import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ───────────────── BACKGROUND COLORS ─────────────────

  /// Main app background
  static const Color kBackground = Color(0xFFF8F5F1);

  /// Dark premium brown
  static const Color kDarkBrown = Color(0xFF2A1F1A);

  /// Coffee brown
  static const Color kCoffeeBrown = Color(0xFF5B3A29);

  /// Mocha brown
  static const Color kMocha = Color(0xFF7B5B46);

  /// Caramel accent
  static const Color kCaramel = Color(0xFFC08A5B);

  /// Light cream background
  static const Color kCream = Color(0xFFF5EEE7);

  /// Beige card background
  static const Color kBeige = Color(0xFFE9DDD1);

  // ───────────────── TEXT COLORS ─────────────────

  static const Color kWhite = Colors.white;

  /// Main title text
  static const Color kTextPrimary = Color(0xFF2B1F1A);

  /// Secondary text
  static const Color kTextSecondary = Color(0xFF8C7B70);

  /// Hint text
  static const Color kHintText = Color(0xFFA8998F);

  // ───────────────── COMPONENT COLORS ─────────────────

  /// Search bar
  static const Color kSearchBar = Color(0xFFF3ECE5);

  /// Coffee category chip background
  static const Color kChipBackground = Color(0xFFF1E9E1);

  /// Selected chip
  static const Color kSelectedChip = Color(0xFFC08A5B);

  /// Bottom navigation
  static const Color kBottomNav = Color(0xFF2F211B);

  /// Card background
  static const Color kCardBackground = Color(0xFFFFFFFF);

  /// Border color
  static const Color kBorder = Color(0xFFE8DED6);

  /// Discount badge
  static const Color kOfferBadge = Color(0xFFE6A15B);

  // ───────────────── BUTTON COLORS ─────────────────

  static const Color kPrimaryButton = Color(0xFF4A342A);

  static const Color kSecondaryButton = Color(0xFFD6B08C);

  // ───────────────── STATUS COLORS ─────────────────

  static const Color kSuccess = Color(0xFF2E9D65);

  static const Color kError = Color(0xFFE45757);

  static const Color kShadow = Color(0x1A000000);
}